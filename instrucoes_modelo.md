Projeto: anasus.

Considere os arquivos do projeto já disponíveis, especialmente:
`contexto.md`, `arquitetura_minima.md`, `catalogo_camadas_fontes.json`, `catalogo_papeis_semanticos.json`, `catalogo_regras_join.json`, `app.R` e arquivos em `R/`.

Regra:
- trabalhar só no módulo solicitado
- preservar o que já funciona
- responder com mudanças pequenas e testáveis
- sempre dizer quais arquivos serão alterados
- gerar o conteúdo completo dos arquivos modificados

Estado atual:
o app Shiny base já lista `dm_`, `tf_`, colunas, detecta campos semânticos, gera SQL, executa consulta e mostra tabela + `analysis_spec`.

Próxima tarefa:
[cole aqui a tarefa do momento]
